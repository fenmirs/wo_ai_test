# 回收站与重名检查 — 改造方案

> 基于 2026-05-20 讨论整理

---

## 一、回收站

### 1.1 数据层 (ProjectManager.js)

| 方法 | 说明 | 状态 |
|------|------|------|
| `softDeleteAPI(apiId)` | 已存在，需增强：软删除时在回收站范围内检查重名，同名则自动追加 `(2)``(3)` 后缀；保留 group 等所有字段 | ⚠️ 需增强 |
| `restoreAPI(apiId)` | 已存在，翻转 `deleted = false`；需增强：检查原分组是否存在，不存在则自动创建 | ⚠️ 需增强 |
| `deleteAPI(apiId)` | 已存在，硬删除（移除索引、缓存、磁盘文件） | ✅ 可复用 |
| `emptyTrash()` | 新增：遍历所有 `deleted = true` 的 API，逐个调 `deleteAPI` | ➕ 新增 |

### 1.2 树节点 (APIMain.js)

#### 回收站分组渲染
- 在所有分组最底部固定显示，可折叠/展开，不可拖拽、不可改名
- 使用垃圾桶图标，样式有别于普通分组
- 回收站下的分组（如存在）也可折叠
- 回收站下的 API 和分组均不可修改（只读）
- 分组名称"回收站"保留，禁止其他分组命名为 "回收站"

#### 已删除 API 展示
- 名称以灰色显示，显示原属分组名称
- 可点击打开详情页（只读模式）
- 可拖拽到正常分组自动恢复（见拖拽规则）
- 没有 `...` 操作菜单，仅提供两个按钮：
  - **恢复**：恢复后如果原分组已不存在，自动创建同名分组；如果目标分组已有同名 API，按重名检查规则自动追加后缀
  - **彻底删除**：调用 `deleteAPI` 硬删除
- 搜索时已删除 API 参与匹配，搜索命中时名称以灰色+删除线显示

#### 清空回收站
- 回收站末尾显示"清空回收站"按钮
- 点击后二次确认，确认后调用 `emptyTrash()`

#### 拖拽规则
- **回收站 → 正常分组**：允许。自动执行恢复（`deleted = false`）+ 移动到目标分组，执行重名检查逻辑
- **正常分组 → 回收站**：允许。相当于软删除（`deleted = true`）
- **回收站内部拖拽**：不允许（回收站内不可修改）
- **正常分组间拖拽**：维持现有行为，增加重名检查

### 1.3 详情页只读模式 (APIDetail.js)

新增 `readOnly` prop，当为 `true` 时：

| 区域 | 行为 |
|------|------|
| API 名称 input | disabled |
| Method 下拉 | disabled |
| URL 路径段 | 不可点击编辑，仅展示文本 |
| 场景切换 | 允许切换查看，禁止新增/删除/改名 |
| Params / Headers / Body / 断言 | 所有输入 disabled |
| 保存 / 发送 / 生成文档 | 隐藏或不显示 |

### 1.4 胶水层 (App.js)

- 新增 `onRestoreAPI` / `onPermanentDelete` / `onEmptyTrash` 回调
- 判断当前选中 API 是否 `deleted`，传递 `readOnly` 给 APIDetail
- 恢复后若原 API 正在打开，刷新视图
- `handleAPISelect` 支持从回收站点击打开（允许选择 `deleted = true` 的 API）

---

## 二、重名检查

### 2.1 检查范围

所有涉及名称变更的操作均需检查**同父级下**是否已存在同名项：

| 操作 | 类型 | 不通过处理 |
|------|------|-----------|
| 添加 API | API | 自动追加后缀（如 `名称 (2)`），toast 提示 |
| 添加分组 | 分组 | 自动追加后缀，toast 提示 |
| API 拖移到新分组 | API | 自动追加后缀，toast 提示 |
| 分组拖移到新父级 | 分组 | 自动追加后缀，toast 提示 |
| 正常分组 → 回收站（软删除） | API | 回收站范围内检查同名，自动追加后缀 |
| API 重命名 | API | **拒绝操作**，toast 提示 |
| 分组重命名 | 分组 | **拒绝操作**，toast 提示 |

### 2.2 追加后缀规则

```
原名称 → 原名称 (2) → 原名称 (3) → ... 直到不重复
```

### 2.3 API 名称作用域

API 名称在同分组内唯一，跨分组可重名。

判断条件：
```js
api.name === newName && api.group === targetGroupId && !api.deleted
```

### 2.4 分组名称作用域

分组名称在同父级下唯一，不同父级可重名。

判断条件：
```js
group.name === newName && group.parentId === targetParentId
```

---

## 三、涉及文件清单

| 文件 | 改动量 | 主要内容 |
|------|--------|----------|
| `src/utils/ProjectManager.js` | 小 | 增强 `restoreAPI`，新增 `emptyTrash` |
| `src/components/APIMain.js` | 大 | 新增回收站分组渲染、已删除 API 列表、恢复/彻底删除/清空按钮、重名检查 |
| `src/components/APIDetail.js` | 大 | 新增 `readOnly` 只读模式，全面禁用编辑交互 |
| `src/components/APIMain.css` | 中 | 回收站样式、已删除 API 样式、按钮样式 |
| `src/components/APIDetail.css` | 小 | 只读状态下输入框的视觉反馈 |
| `src/App.js` | 中 | 新增回调、传递 readOnly、调整选择逻辑 |

---

## 四、实施顺序建议

1. **数据层** — ProjectManager 增强（restoreAPI + emptyTrash）
2. **重名检查** — 所有操作的检查逻辑（可独立测试）
3. **回收站 UI** — APIMain 树渲染 + CSS
4. **只读模式** — APIDetail 改造
5. **胶水层** — App.js 串联所有新功能
