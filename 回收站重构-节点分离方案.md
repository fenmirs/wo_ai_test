# 回收站重构方案：正常节点与软删除节点分离

## 一、问题分析

### 1.1 当前设计缺陷

| 问题 | 描述 |
|------|------|
| **数据耦合** | 正常节点和软删除节点共用同一数组，仅通过 `deleted` 字段区分 |
| **查询复杂** | 所有列表查询都需要 `.filter(a => !a.deleted)` 过滤 |
| **重名处理冗余** | 需要 `_ensureUniqueTrashAPIName` 和 `ensureUniqueAPIName` 两套逻辑 |
| **数据冗余** | 需要 `originalGroupName` 字段记录原始分组信息 |
| **拖拽逻辑混乱** | 拖入回收站设置 `deleted: true`，拖出需要先恢复再移动 |
| **边界情况多** | 删除分组时需递归处理子分组和 API 的 `deleted` 状态 |
| **缓存管理复杂** | `apiDataCache` 需要区分正常和已删除 API |

### 1.2 当前数据结构

```javascript
// config.apis - 混合存储
[
  { id: 'api_1', name: 'A', deleted: false },
  { id: 'api_2', name: 'B', deleted: true },  // 软删除
  { id: 'api_3', name: 'C', deleted: false },
]

// config.groups - 混合存储
[
  { id: 'group_1', name: 'G1', deleted: false },
  { id: 'group_2', name: 'G2', deleted: true },  // 软删除
]
```

## 二、改造目标

1. **结构分离**：`apis` / `groups` 只存正常节点，`trash` 专区存储已删除节点
2. **语义清晰**：删除 = 移动到 trash，恢复 = 从 trash 移出
3. **简化查询**：无需过滤条件直接获取目标列表
4. **独立命名空间**：正常区和回收站各自独立命名约束
5. **拖拽直觉化**：拖入/拖出直接移动数据，无需状态切换

## 三、数据结构设计

### 3.1 新数据结构

```javascript
// project.json 结构
{
  "name": "my-project",
  "apis": [           // 只包含正常 API
    { id: 'api_1', name: 'A', method: 'GET', api_path: '/a', group: 'default' },
    { id: 'api_3', name: 'C', method: 'POST', api_path: '/c', group: 'group_1' }
  ],
  "groups": [         // 只包含正常分组
    { id: 'default', name: '默认', parentId: null },
    { id: 'group_1', name: 'G1', parentId: null }
  ],
  "trash": {
    "apis": [         // 回收站中的 API
      { 
        id: 'api_2', 
        name: 'B', 
        method: 'GET', 
        api_path: '/b', 
        group: 'default',
        deletedAt: 1716182400000,  // 删除时间戳
        originalGroupId: 'default'  // 原始分组 ID
      }
    ],
    "groups": [       // 回收站中的分组
      {
        id: 'group_2',
        name: 'G2',
        parentId: null,
        deletedAt: 1716182400000,
        originalParentId: null
      }
    ]
  }
}
```

### 3.2 apiDataCache 分离

```javascript
// 内存缓存结构
{
  apiDataCache: {
    'api_1': { id: 'api_1', scenarios: {...} },  // 正常
    'api_3': { id: 'api_3', scenarios: {...} },  // 正常
  },
  trashApiDataCache: {
    'api_2': { id: 'api_2', scenarios: {...} }   // 回收站
  }
}
```

### 3.3 文件存储结构

```
project-dir/
├── apis/
│   ├── api_1.json
│   └── api_3.json
└── trash/
    └── apis/
        └── api_2.json    # 回收站的 API 文件
```

## 四、API 设计

### 4.1 ProjectManager 新增/改造方法

#### 4.1.1 API 操作

```javascript
// 软删除 API（移动到 trash）
softDeleteAPI(apiId) {
  const api = this.config.apis.find(a => a.id === apiId);
  if (!api) return;
  
  // 记录原始分组（用于恢复）
  api.originalGroupId = api.group;
  api.deletedAt = Date.now();
  
  // 移动到 trash
  this.config.trash.apis.push(api);
  this.config.apis = this.config.apis.filter(a => a.id !== apiId);
  
  // 移动缓存
  this.trashApiDataCache[apiId] = this.apiDataCache[apiId];
  delete this.apiDataCache[apiId];
  
  this.markDirty();
}

// 恢复 API（从 trash 移出）
restoreAPI(apiId, targetGroupId = null) {
  const api = this.config.trash.apis.find(a => a.id === apiId);
  if (!api) return;
  
  // 检查目标分组是否存在
  if (targetGroupId && targetGroupId !== 'default') {
    const groupExists = this.config.groups.some(g => g.id === targetGroupId);
    if (!groupExists) {
      // 自动重建分组
      this.config.groups.push({
        id: targetGroupId,
        name: api.originalGroupId || targetGroupId,
        parentId: null
      });
    }
  }
  
  // 恢复时处理重名（同分组内唯一）
  const finalGroup = targetGroupId || api.originalGroupId || 'default';
  api.name = this._ensureUniqueName(api.name, finalGroup, 'api');
  api.group = finalGroup;
  delete api.originalGroupId;
  delete api.deletedAt;
  
  // 移出 trash
  this.config.apis.push(api);
  this.config.trash.apis = this.config.trash.apis.filter(a => a.id !== apiId);
  
  // 恢复缓存
  this.apiDataCache[apiId] = this.trashApiDataCache[apiId];
  delete this.trashApiDataCache[apiId];
  
  this.markDirty();
}

// 彻底删除 API
permanentDeleteAPI(apiId) {
  // 从 trash 移除
  this.config.trash.apis = this.config.trash.apis.filter(a => a.id !== apiId);
  
  // 删除缓存
  delete this.trashApiDataCache[apiId];
  delete this.apiHistoryCache[apiId];
  
  // 删除文件
  window.electron.deleteTrashAPIFile(this.dirPath, apiId);
  
  this.markDirty();
}

// 清空回收站
emptyTrash() {
  const trashIds = this.config.trash.apis.map(a => a.id);
  
  // 删除所有 trash API 文件
  trashIds.forEach(id => {
    window.electron.deleteTrashAPIFile(this.dirPath, id);
  });
  
  // 清空数据结构
  this.config.trash.apis = [];
  this.config.trash.groups = [];
  this.trashApiDataCache = {};
  
  this.markDirty();
}

// 名称唯一性检查（基于列表隔离，无需跨列表检查）
_ensureUniqueName(name, groupId, type, excludeId = null, inTrash = false) {
  const list = inTrash ? this.config.trash.apis : this.config.apis;
  // 同组内唯一
  let result = name;
  let counter = 2;
  while (list.some(a => a.name === result && a.group === groupId && a.id !== excludeId)) {
    result = `${name} (${counter++})`;
  }
  return result;
}
```

#### 4.1.2 分组操作

```javascript
// 软删除分组（包含子分组和 API）
softDeleteGroup(groupId) {
  const allGroupIds = this._getGroupAndDescendantIds(groupId, true);
  
  // 1. 移动分组到 trash
  allGroupIds.forEach(id => {
    const group = this.config.groups.find(g => g.id === id);
    if (group) {
      group.deletedAt = Date.now();
      group.originalParentId = group.parentId;
      this.config.trash.groups.push(group);
      this.config.groups = this.config.groups.filter(g => g.id !== id);
    }
  });
  
  // 2. 移动该分组下的 API 到 trash
  const apisToTrash = this.config.apis.filter(a => allGroupIds.includes(a.group));
  apisToTrash.forEach(api => {
    api.originalGroupId = api.group;
    api.deletedAt = Date.now();
    this.config.trash.apis.push(api);
    this.config.apis = this.config.apis.filter(a => a.id !== api.id);
    
    // 移动缓存
    this.trashApiDataCache[api.id] = this.apiDataCache[api.id];
    delete this.apiDataCache[api.id];
  });
  
  this.markDirty();
}

// 恢复分组
restoreGroup(groupId, targetParentId = null) {
  const group = this.config.trash.groups.find(g => g.id === groupId);
  if (!group) return;
  
  // 恢复时重建分组树结构
  const parentId = targetParentId ?? group.originalParentId ?? null;
  group.parentId = parentId;
  delete group.deletedAt;
  delete group.originalParentId;
  
  // 处理分组重名
  group.name = this._ensureUniqueGroupName(group.name, parentId);
  
  this.config.groups.push(group);
  this.config.trash.groups = this.config.trash.groups.filter(g => g.id !== groupId);
  
  // 恢复该分组下的 API
  const apisToRestore = this.config.trash.apis.filter(a => a.group === groupId);
  apisToRestore.forEach(api => {
    api.group = groupId;
    delete api.originalGroupId;
    delete api.deletedAt;
    
    this.config.apis.push(api);
    this.config.trash.apis = this.config.trash.apis.filter(a => a.id !== api.id);
    
    // 恢复缓存
    this.apiDataCache[api.id] = this.trashApiDataCache[api.id];
    delete this.trashApiDataCache[api.id];
  });
  
  // 递归恢复子分组
  const children = this.config.trash.groups.filter(g => g.originalParentId === groupId);
  children.forEach(child => this.restoreGroup(child.id, groupId));
  
  this.markDirty();
}

// 彻底删除分组
permanentDeleteGroup(groupId) {
  // 获取所有子分组 ID
  const allGroupIds = this._getGroupAndDescendantIds(groupId, true, true);
  
  // 从 trash 移除所有相关分组
  this.config.trash.groups = this.config.trash.groups.filter(g => !allGroupIds.includes(g.id));
  
  // 删除相关 API 缓存和文件
  this.config.trash.apis
    .filter(a => allGroupIds.includes(a.group))
    .forEach(api => {
      delete this.trashApiDataCache[api.id];
      window.electron.deleteTrashAPIFile(this.dirPath, api.id);
    });
  
  // 从 trash 移除相关 API
  this.config.trash.apis = this.config.trash.apis.filter(a => !allGroupIds.includes(a.group));
  
  this.markDirty();
}
```

### 4.2 查询方法简化

```javascript
// 获取正常 API（无需过滤）
getAPIs() {
  return this.config.apis;
}

// 获取回收站 API
getTrashAPIs() {
  return this.config.trash?.apis || [];
}

// 获取正常分组
getGroups() {
  return this.config.groups;
}

// 获取回收站分组
getTrashGroups() {
  return this.config.trash?.groups || [];
}

// 获取分组树（只处理正常分组）
getGroupTree() {
  const rootGroups = this.config.groups.filter(g => !g.parentId);
  const buildTree = (parentId) => {
    return this.config.groups
      .filter(g => g.parentId === parentId)
      .map(g => ({ ...g, children: buildTree(g.id) }));
  };
  return rootGroups.map(g => ({ ...g, children: buildTree(g.id) }));
}
```

## 五、UI 交互改造

### 5.1 拖拽交互

#### 5.1.1 拖入回收站
- 拖拽正常 API/分组到回收站区域
- 直接移动数据到 `trash` 数组
- 更新 UI 显示（自动消失）
- 显示 toast 提示："已移入回收站"

#### 5.1.2 从回收站拖出
- 拖拽回收站中的项目到正常分组
- 直接恢复数据到目标分组
- 如果目标分组存在同名项，自动重命名
- 显示 toast 提示："已恢复至 [分组名]"

#### 5.1.3 拖拽示意改进
```javascript
// 拖拽预览样式
const dragPreview = document.createElement('div');
dragPreview.textContent = isTrashItem ? `📁 ${item.name}` : item.name;
dragPreview.style.borderColor = isTrashItem ? '#f59e0b' : '#3b82f6';
```

### 5.2 回收站 UI

#### 5.2.1 分区展示
```
┌─────────────────────────────────┐
│ 📂 正常区域                      │
│   ├─ 📁 分组 A                   │
│   │   └─ 🌐 API 1                │
│   └─ 🌐 API 2                   │
├─────────────────────────────────┤
│ 🗑️ 回收站（3 项）                │
│   ├─ 📁 已删除分组               │
│   │   └─ 🌐 已删除 API           │
│   └─ 🌐 孤立已删除 API           │
└─────────────────────────────────┘
```

#### 5.2.2 回收站操作
- **恢复**：恢复到指定分组（可选择目标）
- **彻底删除**：直接删除
- **清空回收站**：一键清空所有
- **拖拽移动**：从回收站拖出到目标分组

### 5.3 场景操作隔离

场景（Scenarios）作为 API 的子数据，不进入回收站体系：
- 删除 API → API 及其所有场景进入回收站
- 删除场景 → 直接从 `scenarios` 对象中移除，不进回收站

## 六、文件操作改造

### 6.1 文件结构

```
project-dir/
├── project.json        # 主配置（不含 trash 数据）
├── profile.json        # 环境配置
├── apis/
│   ├── api_1.json
│   └── api_2.json
└── trash/
    └── apis/
        ├── api_trash_1.json
        └── api_trash_2.json
```

### 6.2 保存逻辑

- **正常 API**：保存到 `apis/` 目录
- **回收站 API**：保存到 `trash/apis/` 目录
- **project.json**：不存储 trash 数据（由文件目录结构体现）

### 6.3 加载逻辑

```javascript
async loadProject(dirPath) {
  // 1. 加载 project.json
  const config = await window.electron.loadProjectConfig(dirPath);
  
  // 2. 扫描 apis/ 目录获取正常 API
  const apiFiles = await window.electron.listAPIFiles(dirPath);
  // 加载每个 API 文件到 apiDataCache
  
  // 3. 扫描 trash/apis/ 目录获取回收站 API
  const trashFiles = await window.electron.listTrashAPIFiles(dirPath);
  // 加载到 trashApiDataCache
  
  // 4. 重建 trash.apis 和 trash.groups 列表
  // 基于 trash 目录中的文件 + project.json 中的 trash 元数据
}
```

## 七、改造影响范围

### 7.1 需要修改的文件

| 文件 | 改动内容 |
|------|----------|
| `ProjectManager.js` | 核心数据结构重构，所有 CRUD 方法重写 |
| `App.js` | 删除/恢复/彻底删除/清空回收站 handler 改造 |
| `APIMain.js` | UI 渲染逻辑改造，拖拽逻辑改造 |
| `APIMain.css` | 回收站样式调整 |
| `electron/ipcHandlers.js` | 文件操作 IPC 改造（移动文件到 trash 目录） |

### 7.2 需要新增的方法/文件

| 新增项 | 说明 |
|--------|------|
| `trashApiDataCache` | 回收站 API 缓存 |
| `moveToTrash()` | 移动 API 到 trash |
| `moveToTrashGroup()` | 移动分组到 trash |
| `restoreFromTrash()` | 从 trash 恢复 |
| `deleteTrashFile()` | 删除 trash 文件 |
| `moveTrashFile()` | 移动文件到 trash 目录 |

### 7.3 需要删除的逻辑

| 删除项 | 说明 |
|--------|------|
| `deleted` 字段 | 不再需要 |
| `originalGroupName` 字段 | 不再需要 |
| `_ensureUniqueTrashAPIName()` | 回收站独立唯一 |
| 跨列表重名检查 | 不再需要 |

## 九、测试用例

### 9.1 API 操作测试

| 用例 | 预期结果 |
|------|----------|
| 删除 API | API 从列表消失，出现于回收站 |
| 恢复 API | API 回到原分组，缓存恢复 |
| 彻底删除 | API 从回收站永久消失 |
| 清空回收站 | 所有已删除项彻底删除 |
| 删除同名 API A、B，再恢复 B | B 正常恢复，A 仍在回收站 |

### 9.2 分组操作测试

| 用例 | 预期结果 |
|------|----------|
| 删除分组（包含子分组和 API） | 整个子树移入回收站 |
| 恢复分组 | 分组树和 API 都恢复 |
| 删除后恢复，再删除同名分组 | 两个分组都在回收站，独立存在 |

### 9.3 拖拽测试

| 用例 | 预期结果 |
|------|----------|
| 拖拽 API 到回收站 | 移动成功，列表更新 |
| 拖拽回收站 API 到分组 | 恢复成功，回收站更新 |
| 拖拽分组到回收站 | 整个子树移动 |
| 拖拽回收站分组到其他分组 | 恢复并重建父子关系 |

### 9.4 边界测试

| 用例 | 预期结果 |
|------|----------|
| 删除后修改 API 配置 | 配置保存在 trash 缓存 |
| 恢复后继续编辑 | 正常编辑流程 |
| 彻底删除后打开项目 | 数据一致，无残留 |

## 十、实施计划

### Phase 1: 数据结构重构
- [ ] 修改 `project.json` 结构
- [ ] 重构 `ProjectManager` 核心方法
- [ ] 实现文件目录迁移

### Phase 2: UI 改造
- [ ] 重构 `APIMain` 渲染逻辑
- [ ] 改造拖拽交互
- [ ] 调整样式

### Phase 3: 兼容性
- [ ] 实现旧数据迁移
- [ ] 测试迁移完整性

### Phase 4: 联调测试
- [ ] 全流程测试
- [ ] 边界情况测试
- [ ] 性能测试
